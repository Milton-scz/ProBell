<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".View.NotaVentaView.NotaVentaViewActivity">

    <!-- RecyclerView para mostrar la lista de categorías -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewNotaVentas"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginBottom="64dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/btnCrearNotaVenta"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"/>

    <!-- Botón para crear una nueva nota de venta -->
    <Button
        android:id="@+id/btnCrearNotaVenta"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Crear Nota Venta"
        app:layout_constraintBottom_toTopOf="@+id/btnVerDetalleNotaVenta"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_chainStyle="packed"
        android:layout_marginBottom="16dp"/>

    <!-- Botón para ver la lista de detalles de nota de venta -->
    <Button
        android:id="@+id/btnVerDetalleNotaVenta"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Ver Detalles Nota Venta"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_chainStyle="packed"
        android:layout_marginBottom="16dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>
